## 2023-10-27 - 优化分页组件可访问性
**Learning:** 分页组件经常包含仅有图标的上一页/下一页按钮，且当前页通常仅通过颜色区分，这对屏幕阅读器用户极不友好。
**Action:** 下次遇到分页组件时，务必使用 `<nav aria-label="分页">` 包装，并为图标按钮添加 `aria-label`，为当前选中页添加 `aria-current="page"`。
